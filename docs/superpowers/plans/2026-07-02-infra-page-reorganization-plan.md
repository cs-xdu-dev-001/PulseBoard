# Infra 页面整理实施计划

## 目标

把原本拥挤的 Infra 页面拆成总览和详情页，降低首页信息密度。

## 主要任务

1. 在 Infra 内部增加二级 Tab。
2. 新增 `Overview`，只显示关键状态和摘要。
3. 将完整 GPU 列表和 GPU 历史图移动到 `GPU`。
4. 将 VPS 卡片和 VPS 历史图移动到 `VPS`。
5. 将实验室机器卡片和机器历史图移动到 `Machines`。
6. 补充二级导航和总览卡片样式。

## 验证

- 前端构建
- 本地页面刷新检查
